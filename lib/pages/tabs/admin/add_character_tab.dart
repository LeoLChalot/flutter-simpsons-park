import 'package:flutter/material.dart';

class AddCharacterTab extends StatelessWidget {
  const AddCharacterTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
